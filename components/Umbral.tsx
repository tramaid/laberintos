'use client';
/* SECCIÓN 1 — EL UMBRAL
   Contenedor de 240vh + hijo sticky. Dos tomas fotográficas encadenadas
   con un corte seco escondido en un destello.

   LEER SPEC §3 ANTES DE TOCAR ESTO. Los seis puntos de "reglas no
   negociables" son errores que ya se cometieron y se corrigieron midiendo.

   TODO(sesión de animación): implementar. En la sesión de layout esto es
   simplemente la primera capa quieta, a pantalla completa. */
export default function Umbral() {
  return (
    <section className="umbral" aria-label="Entrada">
      {/* TODO: capas de public/hero/ + destello + título */}
    </section>
  );
}
